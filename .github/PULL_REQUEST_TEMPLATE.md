## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck` and `npm test` pass
- [ ] Any new user-facing string is in `src/shared/i18n/locales/en.json` and every other locale
- [ ] Behaviour that can strand a user (unlock paths, duration limits) is covered by a test
- [ ] Screenshots refreshed with `npm run screenshots` if the UI changed
