{
  "name": "keyboard-lock",
  "productName": "Keyboard Lock",
  "version": "1.0.0",
  "description": "Lock your keyboard for a set time so you can wipe it clean. Dark, slim, cross-platform.",
  "license": "MIT",
  "author": {
    "name": "Keyboard Lock Contributors"
  },
  "homepage": "https://github.com/OWNER/keyboard-lock",
  "repository": {
    "type": "git",
    "url": "https://github.com/OWNER/keyboard-lock.git"
  },
  "bugs": {
    "url": "https://github.com/OWNER/keyboard-lock/issues"
  },
  "keywords": [
    "keyboard",
    "lock",
    "clean",
    "cleaning",
    "electron",
    "cross-platform",
    "utility",
    "dark-mode"
  ],
  "main": "out/main/index.js",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "build": "npm run typecheck && electron-vite build",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "icons": "node scripts/generate-icon.mjs",
    "screenshots": "node scripts/capture-screenshots.mjs",
    "dist": "npm run build && electron-builder",
    "dist:mac": "npm run build && electron-builder --mac",
    "dist:win": "npm run build && electron-builder --win",
    "dist:linux": "npm run build && electron-builder --linux",
    "dist:dir": "npm run build && electron-builder --dir"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.4.1",
    "electron": "^44.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.69.0",
    "vite": "^7.3.6",
    "vitest": "^5.0.0"
  }
}
